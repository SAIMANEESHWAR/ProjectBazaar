# Lambda function dependencies
# Install in a layer or bundle with the deployment package

# PDF extraction (PyPDF2 is the most compatible with Lambda)
PyPDF2>=3.0.0

# Optional: Better PDF extraction for complex layouts
# pdfplumber>=0.10.0

# Note: This Lambda includes a basic PDF extractor using Python's built-in zlib
# that works without any external dependencies. PyPDF2 is recommended for better results.
