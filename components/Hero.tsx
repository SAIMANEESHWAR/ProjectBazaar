import React from 'react';
import { InterviewPrepBlackHero } from './sections/InterviewPrepBlackHero';

const Hero: React.FC = () => {
  return (
    <header className="relative w-full overflow-hidden bg-black">
      <InterviewPrepBlackHero />
    </header>
  );
};

export default Hero;
